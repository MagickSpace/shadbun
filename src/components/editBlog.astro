{
  process.env.NODE_ENV === 'development' && (
    <a
      href={`http://localhost:4321/admin/index.html`}
      target="_blank"
      class="fixed bottom-5 right-5 z-50 flex w-[120px] items-center justify-center rounded-full border-2 border-gray-500 bg-white py-[2.5px] text-[12px] font-bold uppercase text-black shadow-2xl"
    >
      Edit in CMS
    </a>
  )
}
